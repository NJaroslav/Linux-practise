Print the names of all users on the system from the /etc/passwd file, separated by commas.
The output should end with a line break.

- ### Example:
```
$ ./usernames
root,daemon,bin,sys,sync,games,student
```
